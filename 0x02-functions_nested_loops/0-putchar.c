#include <unistd.h>

/**
 * main - prints the string "Holberton" from a character array.
 * Return: 0 on success.
 */

int main(void)
{
	const char *message = "_putchar\n"
		write(1, message, 9);
	return (0);
}
